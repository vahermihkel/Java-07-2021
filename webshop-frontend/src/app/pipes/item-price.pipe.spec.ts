import { ItemPricePipe } from './item-price.pipe';

describe('ItemPricePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemPricePipe();
    expect(pipe).toBeTruthy();
  });
});
